// MAP com strings
 
let nomes = ['Danilo', 'Maria', 'José', 'Ana']

const nomesAtualizados = nomes.map(nome => nome.toUpperCase()) // utilizando o toUpperCase, para padronizar tudo em MAIUSCULO.

console.log(nomesAtualizados)